# buckets

This repository is set up as a **pnpm workspace** for publishing npm packages from the `packages/` directory.

## Workspace structure

- `packages/buckets` – example publishable package built with **tsup**

## Build

```bash
pnpm install
pnpm build
```

## Lint and format

```bash
pnpm lint
pnpm format
```
